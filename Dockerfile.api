FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY server ./server
COPY CHANGELOG.md ./

ENV NODE_ENV=production
ENV API_PORT=4199

EXPOSE 4199

CMD ["node", "server/index.mjs"]
