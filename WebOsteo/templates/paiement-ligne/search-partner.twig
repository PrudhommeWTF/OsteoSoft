{% if id in ['cashlee','stripe','sumup'] %}
    <span><img src="{{ logo }}" style="width: 35px;height: auto"></span>
    {{ sous_titre }} <span class="label label-info">Nouveau</span>
{% else %}
    <span>{{ libelle }}</span>
{% endif %}
