<div class="col-xs-12">
    <p>
        {{ message }}

        {% if temp_password  is not empty %}
            <br>Voici le code à transmettre au patient : <span class="rose">{{ temp_password }}</span>.
        {% endif %}
    </p><br/>
    <a href="{{ url }}" target="_blank" class="btn btn-primary roundFacture"><i class="fa fa-external-link"></i> Voir la fiche sur Andrew®</a>
</div>
<div class="clear"></div>